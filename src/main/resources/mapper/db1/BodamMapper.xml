<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.server.model.dao.BodamMapper">

    <insert id="insertBodam" parameterType="Bodam" useGeneratedKeys="true" keyProperty="bodamId">
        INSERT INTO bodams (bodam_id, bodam_name, bodam_gender, communication_ability, cognitive_ability, performance_ability)
        VALUES (#{bodamId}, #{bodamName}, #{bodamGender}, #{communicationAbility}, #{cognitiveAbility}, #{performanceAbility})
    </insert>

    <select id="selectBodamByUser" parameterType="int" resultType="Bodam">
        SELECT *
        FROM bodams
        WHERE bodam_id=#{bodamId}
    </select>

</mapper>