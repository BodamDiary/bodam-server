<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.server.model.dao.UserMapper">
    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (email, kakao_id, nickname, user_name, password, profile_image, address, phone_number, agree_condition, created_at)
        VALUES (#{email}, #{kakaoId}, #{nickname}, #{userName}, #{password}, #{profileImage}, #{address}, #{phoneNumber}, #{agreeCondition}, #{createdAt})
    </insert>

    <select id="selectUser" parameterType="String" resultType="User">
        SELECT * FROM users
        WHERE email=#{email}
    </select>

</mapper>